# dev/

Scratch scripts, sample inputs and rendered documents kept from the `ermeeth`
package. These are **not** unit tests: they were sitting in `tests/`, where
`R CMD check` would have tried to execute them. They are excluded from the
build via `.Rbuildignore`.

Real tests live in `tests/testthat/`.
